<form method="post" action="{{ path('app_application_day_quest_delete', {'id': day_quest.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ day_quest.id) }}">
    <button class="btn">Delete</button>
</form>
