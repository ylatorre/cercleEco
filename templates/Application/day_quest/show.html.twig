{% extends 'base.html.twig' %}

{% block title %}DayQuest{% endblock %}

{% block body %}
    <h1>DayQuest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ day_quest.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ day_quest.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ day_quest.description }}</td>
            </tr>
            <tr>
                <th>Xp</th>
                <td>{{ day_quest.xp }}</td>
            </tr>
            <tr>
                <th>Date_de_creation</th>
                <td>{{ day_quest.dateDeCreation ? day_quest.dateDeCreation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_application_day_quest_index') }}">back to list</a>

    <a href="{{ path('app_application_day_quest_edit', {'id': day_quest.id}) }}">edit</a>

    {{ include('application/day_quest/_delete_form.html.twig') }}
{% endblock %}
