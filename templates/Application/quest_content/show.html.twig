{% extends 'base.html.twig' %}

{% block title %}QuestContent{% endblock %}

{% block body %}
    <h1>QuestContent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quest_content.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ quest_content.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_application_quest_content_index') }}">back to list</a>

    <a href="{{ path('app_application_quest_content_edit', {'id': quest_content.id}) }}">edit</a>

    {{ include('application/quest_content/_delete_form.html.twig') }}
{% endblock %}
