<form method="post" action="{{ path('app_front_dons_delete', {'token': don.token}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ don.token) }}">
    <button class="bg-red-500 text-white py-2 px-4 rounded hover:bg-red-700">Supprimer</button>
</form>
