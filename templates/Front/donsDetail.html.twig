{% extends 'base.html.twig' %}

{% block title %}Greenify - Accueil{% endblock %}

{% block body %}
{# <<<<<<< Updated upstream #}
<div class="min-h-screen bg-[#3B4419]">
    <div class="max-w-7xl mx-auto px-6 py-10">
        <div class="flex justify-end">
            <a href="{{path('app_front_dons_new')}}" class="flex items-center bg-[#DFE777] text-[#3B4419] p-2 rounded-full hover:bg-[#B8BC86] transition">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
                </svg>
            </a>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            <div class="bg-[#DFE777] p-6 rounded-lg flex flex-col items-center text-center text-[#3B4419]">
                {% if don.image %}
                    <img src="{{ asset('image/dons/' ~ don.image) }}" alt="{{ don.titre }}" class="w-full h-48 object-cover mb-4 rounded-lg">
                {% endif %}
                <h2 class="text-2xl font-serif mb-4">{{ don.titre }}</h2>
                <p class="text-gray-700 mb-4">{{ don.description }}</p>
                <p class="text-gray-600 mb-4">Emplacement : {{ don.localisation ? don.localisation : 'Non spécifié' }}</p>   
            </div>
{# <div class="min-h-screen bg-[#3B4419] text-[#DFE777]">
    <div class="max-w-7xl mx-auto px-6 py-10 flex flex-col md:flex-row-reverse items-start gap-10">

        <!-- Product Image Section - Placed on the Right -->
        <div class="w-full md:w-1/2 flex justify-center items-center">
            {% if don.image %}
                <img src="{{ asset('image/dons/' ~ don.image) }}" alt="{{ don.titre }}" class="object-cover   h-auto rounded-lg shadow-lg">
            {% else %}
                <p class="text-center text-gray-500">Image non disponible</p>
            {% endif %}
>>>>>>> Stashed changes #}
        {# </div> #}

        <!-- Product Details Section - Placed on the Left -->
        <div class="w-full bg-[#DFE777] p-10 rounded-lg text-[#3B4419]">
            <h1 class="text-4xl font-serif mb-6">{{ don.titre }}</h1>
            <p class="text-lg mb-8">{{ don.description }}</p>

            <!-- Advantages Section -->
            <h3 class="text-2xl font-semibold mb-4">Avantages :</h3>
            <ul class="list-disc list-inside space-y-2 mb-8">
                <li><strong>Esthétique Moderne</strong> : Idéale pour ceux qui apprécient l'ameublement  moderne et design.</li>
                <li><strong>Polyvalence</strong> : Parfaite pour le salon, les etudes , le café, et plus encore.</li>
                <li><strong>Touche Contemporaine</strong> : Ajoute une touche contemporaine à votre cuisine ou espace de dégustation.</li>
                <li><strong>Durabilité</strong> : Conçue pour durer, cette chaise résiste aux chocs et à l'usure quotidienne.</li>
            </ul>

            <!-- Usage Section -->
            <h3 class="text-2xl font-semibold mb-4">Utilisation :</h3>
            <ul class="list-disc list-inside space-y-2 mb-8">
                <li><strong>Petit-Déjeuner</strong> : Commencez votre journée avec style.</li>
                <li><strong>Goûter</strong> : Parfait pour une pause gourmande.</li>
                <li><strong>Cadeau</strong> : Un excellent cadeau pour les amateurs de design et d'artisanat.</li>
            </ul>

            <!-- Add to Cart Button -->
            <button class="mt-4 px-6 py-3 border border-[#3B4419] bg-transparent rounded-full text-[#3B4419] font-semibold hover:bg-white hover:text-[#DFE777] transition-colors">
                Je reserve 
            </button>
        </div>
        
    </div>
</div>
{% endblock %}

